# Core runtime dependencies for the Streamlit scouting prototype
streamlit==1.38.0
pandas==2.3.3
altair==5.3.0
numpy==1.26.4  # Pinned to <2.0 for compatibility with scipy/xgboost
openai==1.54.4  # Updated to latest stable version with improved API support
httpx==0.27.2   # Pin httpx to avoid compatibility issues with OpenAI SDK
pyarrow==18.0.0
lightgbm==4.5.0
scikit-learn==1.7.2
scipy==1.14.1  # Pinned for compatibility with numpy<2.0
xgboost==2.0.3  # Pinned to 2.0.x for compatibility with SHAP 0.49.1
shap==0.49.1  # Pinned to <0.50.0 for compatibility with numpy<2.0
joblib==1.5.2
matplotlib==3.8.1
seaborn
pathlib

